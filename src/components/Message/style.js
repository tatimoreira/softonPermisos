import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
    root: {
        display: 'flex',
        flexWrap: 'wrap',
      },
      noRequest: {
        padding: '10px',
        marginTop: '10px'
      },
      container_centered: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      },
});